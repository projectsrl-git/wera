# 2026-07-19 — Ondata 2: Anagrafiche (inserimento + ricerca)

## Obiettivo
Portare al nuovo design le 20 pagine anagrafiche (inserimento/ricerca di utenze, condomini,
contatori, antenne, tecnici, software; aziende, utenti, profili, domini) SENZA toccare i template:
il tema copriva gia il 95%, l'ondata aggiunge solo rifiniture CSS (sezione 21).

## File toccati
- `src/main/webapp/css/wera-theme.css` — nuova sezione 21:
  - `.ibox-title.titolo` (banda titolo pagina) su token invece dei grigi hardcoded di bow.css
  - `.control-label-left` allineata tipograficamente alle control-label
  - bottoni export `#report/#excel/#csv` da gradiente a ghost (il solo "Ricerca" resta primario)
  - `fieldset` legenda come chip discreta (pattern identico in tutte le 36 pagine che la usano)
  - icone riga `editRow/deleteRow/pdfA4Row/viewRow` con hover colorato per azione
  - `.tabs-container .panel-body .ibox` de-cardizzato (evita card-nella-card in inserimentoaziende)
  - skin tipografica SweetAlert2 (`.swal2-popup`) — i colori inline dei dialoghi restano invariati
- NESSUN template modificato: censiti tutti e 20, zero stili inline, markup omogeneo gia coperto.

## Componenti: prima -> dopo
- Barra azioni ricerca: 4 bottoni gradiente identici -> 1 primario + 3 ghost export
- Legenda icone: testo nudo 11px -> chip su surface-2 con bordo
- Banda titolo: #fbfbfd/#e7e9eb -> var(--surface)/var(--line)
- Tab "Dati azienda": doppia cornice -> pannello unico pulito

## Cosa NON e stato toccato (conferma)
Logica, config.cfg, SQL, FreeMarker `${...}`/`<#...>`, cablaggio form (name/id/action/FUNCTIONID/
OPERATION_TYPE), handler, id DataTables/select2/datepicker: invariati. Encoding invariati
(6 ricerca* sono ISO-8859-1: non riscritte).

## QA
- [ ] una ricerca* (es. ricercaanagraficacondomini): filtri colonna, ricerca in linea, export PDF/XLS/CSV, matita/cestino
- [ ] un inserimento* (es. inserimentoanagraficacondomini): select2, datepicker, salvataggio con Swal
- [ ] inserimentoaziende: tab DATI AZIENDA / LEGALE RAPPRESENTANTE
- [ ] inserimentoprofili: multiselect LISTA_MENU_ABILITATI
- Note: gli id #report/#excel/#csv sono presenti in 24/26 ricerca* -> lo stile ghost vale anche per ondate 3/5/6.

## Rischi / punti aperti
- Regola `form fieldset` volutamente generica: verificato che TUTTI i fieldset dell'app sono il pattern legenda.
- Nessun punto saltato.
