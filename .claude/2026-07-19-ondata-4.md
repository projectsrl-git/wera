# 2026-07-19 — Ondata 4: Configurazione guidata (wizard 0..6)

## Obiettivo
Restyle dei 9 template della configurazione guidata (entry, step 0..6, riepilogo) in modalita
conservativa: SOLO CSS (sezione 23), nessun template toccato.

## Analisi
- Il "wizard" e jquery.steps usato come puro indicatore di avanzamento: ogni pagina e uno step
  del flusso e un `<style>` di pagina nasconde content/actions del plugin. Riskinnati solo i
  4 stati (current/done/disabled/error) come pill brand, via override del plugin CSS.
- `#progressBar` (step 0 e 5) e stilato da un `<style>` di pagina verde INSPINIA che vince sul
  tema per ordine di caricamento: superato con selettori a specificita maggiore
  (`div#progressBar > div`), barra su gradiente brand. La larghezza resta pilotata dal JS.
- Dropzone (`form#my-awesome-dropzone`) riskinnata: superficie soft, bordo dashed, hover accento.
  La regola copre anche import* e importfilemanager dell'ondata 5.
- configurazioneguidata6 = variante di inserimentoripartizioni -> gia coperta dalla sezione 22;
  configurazioneguidata0riepilogo = pattern ricerca -> gia coperta dalla sezione 21.

## File toccati
- `src/main/webapp/css/wera-theme.css` — nuova sezione 23 (steps, progressBar, dropzone).
- NESSUN template modificato.

## Cosa NON e stato toccato (conferma)
Logica, FreeMarker, cablaggio form, `$("#wizard").steps()`, dropzone JS, i-checks, datepicker:
invariati. configurazioneguidata0riepilogo (encoding non-UTF8) non riscritto.

## QA
- [ ] configurazioneguidata: select tipo + "Inizia"
- [ ] step 1..4: indicatore step con stato corrente a gradiente; form e salvataggi ok
- [ ] step 0 e 5: upload dropzone funzionante, barra avanzamento visibile durante import
- [ ] step 6: tab ripartizione ok (vedi QA ondata 3)
- [ ] riepilogo: DataTable + export ok

## Rischi / punti aperti
- Gli step del wizard hanno larghezza 25% dal plugin (6 step -> 2 righe): comportamento storico
  NON alterato di proposito (cambiarlo richiederebbe override dimensionali con rischio overflow
  su viewport stretti).
- Nessun punto saltato.
