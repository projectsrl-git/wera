# 2026-07-17 — Ondata 1c: home (metric card unificate) + profilo

## Obiettivo
Chiudere l'ondata 1 portando la home al pattern del demo: per ogni indicatore un'unica card con numero grande, separatore e lista di dettaglio — al posto delle due righe separate (numeri sopra, "Dettaglio" sotto).

## File toccati
- `src/main/webapp/WEB-INF/template/home.html` — le due row (4 ibox numero + 4 ibox Dettaglio) sostituite da una sola `row.metricrow` con 4 card unificate. TUTTI i 23 id popolati da `fetchData` (`div_utenze`, `div_condomini`, `div_ripartitori`, `div_antenne`, e i 19 di dettaglio) preservati, ciascuno presente esattamente una volta (verificato via script). Rimosse le righe segnaposto vuote (`fa-circle text-white`, nessun id/hook). Corretto di passaggio il nesting rotto della 4a colonna della vecchia row 1. Encoding ISO-8859-1 preservato (edit byte-level).
- `src/main/webapp/css/wera-theme.css` — sez. 20: `.metricrow` in flex per colonne a pari altezza, `.metric-sep`, rifiniture tabella dettaglio; breakpoint 2-col/1-col.
- `profilo.html` — NESSUNA modifica: usa gli include condivisi e il tema lo copre già via CSS (tabs, form-horizontal, ibox).

## Cosa NON e stato toccato (conferma)
Script `fetchData`/amcharts, form `formEdit`, blocco `<#if>` FreeMarker, DataTable `#dt` "Richieste di ripartizione", include: invariati. Nessun id rinominato o rimosso.

## QA
- [ ] home: i 4 numeri e tutte le voci di dettaglio si popolano (verificare a occhio contro i valori noti)
- [ ] card a pari altezza; 2 colonne sotto i 1200px, 1 colonna su mobile
- [ ] grafici amcharts e DataTable Richieste invariati
- [ ] profilo: tab Profilo/Crediti ok, salvataggio ok

## Rischi / punti aperti
- Se `fetchData` fallisse su un id la cella resta vuota (comportamento identico a prima).
- Con l'ondata 1c l'ondata 1 è COMPLETA (login/landing, registrazione, reset, newpassword, home, profilo). Prossima: ondata 2 (anagrafiche).
