# 2026-07-17 — Ondata 1b: registrazione, resetpassword, newpassword

## Obiettivo
Completare le pagine auth con lo stile card centrata (`.auth-*`, wera-theme sez. 19): coerenti tra loro e con la landing di login.

## File toccati
- `src/main/webapp/WEB-INF/template/registrazione.html` — presentazione riscritta; hint sotto il campo Rilevatore ("Il codice stampato sul tuo ripartitore"); logo cliccabile -> Login.
- `src/main/webapp/WEB-INF/template/resetpassword.html` — presentazione riscritta; sottotitolo esplicativo aggiunto; bottone "Reimposta password".
- `src/main/webapp/WEB-INF/template/newpassword.html` — presentazione riscritta; lo `<script>` di validazione originale è preservato BYTE PER BYTE; `#INFO_MESSAGE` e `#ERROR_MESSAGE` restano come contenitori con gli stessi id (ora chip colorati, nascosti se vuoti via `:empty`); requisiti password mostrati nel sottotitolo.
- `src/main/webapp/css/wera-theme.css` — sez. 19b: `.auth-info`, `.auth-sub`.

## Cosa NON e stato toccato (conferma)
Tutti i form identici: action/method/id (`Registrazione`/`ResetPassword`/`NewPassword`), hidden FUNCTIONID, SESSION_TOKEN e i due blocchi ERROR_MESSAGE FreeMarker di newpassword (entrambi preservati), name/id/placeholder/required di tutti i campi (NOME, COGNOME, EMAIL, RILEVATORE, USERNAME, OLD_PASSWORD, NEW_PASSWORD, PASSWORD), href, ordine script in coda.

## QA
- [ ] registrazione: submit con tutti i campi -> flusso invariato
- [ ] resetpassword: submit email -> flusso invariato
- [ ] newpassword: submit -> flusso invariato; se il backend valorizza INFO/ERROR_MESSAGE i chip compaiono
- [ ] link incrociati Login/Registrati funzionano
- [ ] responsive 360px

## Rischi / punti aperti
- Bug PREESISTENTI in newpassword, lasciati intatti di proposito (comportamento invariato): la funzione `newPassword()` referenzia `OLDPASSWORD`/`NEWPASSWORD` senza underscore (i campi si chiamano `OLD_PASSWORD`/`NEW_PASSWORD`) e comunque nessuno la invoca (il bottone è un submit semplice): la validazione JS è codice morto, vale la `required` HTML5. Da sistemare in un intervento funzionale, non nel restyling.
- `label for` non allineati agli id dei campi (preesistente, invariato).
