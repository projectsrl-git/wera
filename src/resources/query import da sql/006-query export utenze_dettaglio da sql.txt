SELECT ROW_NUMBER() over (order by daggancio,stanza) as id_dettaglio, 
coalesce((select id_modulo from export_2 where export_2.taggancio=daggancio),0) as id_modulo, 
ROW_NUMBER() over (order by daggancio,stanza) as nr_dettaglio, 
iif(stanza='','STANZA',stanza) as stanza, 
tipo, 
marca, 
larghezza, 
       altezza, 
	   profondita, 
	   elementi, 
	   potenza, 
	   esp, 
	   esp_2, 
	   etichetta as rilevatore, 
       n_prog, 
	   coeff, 
	   pos, 
	   diametro, 
	   mat_tubo, 
	   prereg, 
	   tipo_valvola, 
       '' as tabella_parent, 
	   '' as id_dettaglio_parent, 
	   '2018-09-26 16:00:51.671' as ts_ins, 
	   1 as id_utente_ins, 
	   '' as ts_mod, 
       '' as id_utente_mod
  FROM utenze_dettaglio;